import PropertyForm from "../components/PropertyForm";

export const metadata = {
  title: "Add New Property | Estates Admin",
};

export default function NewPropertyPage() {
  return <PropertyForm />;
}
